// Kept as the existing import target for backwards compatibility.
// The implementation lives in errorHandler so the application has one
// terminal error handler and one logging path.
module.exports = require("./errorHandler");
