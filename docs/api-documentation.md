# API Documentation (Legacy Entry Point)

This legacy page is retained only to avoid breaking existing links.

Use [API.md](API.md) for the current mounted-route inventory, Swagger/OpenAPI locations, authentication conventions, and generated-artifact counts.

Do not restore route counts or contracts here. The canonical human-readable documentation is `docs/API.md`; generated schemas remain in `src/docs/openapi.js`, `docs/openapi.json`, and `docs/postman/`.
